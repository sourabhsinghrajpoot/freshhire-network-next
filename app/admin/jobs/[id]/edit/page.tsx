import { requireAdmin } from '@/lib/auth'; import { connectDb } from '@/lib/db'; import { Job } from '@/models/Job'; import { JobForm } from '@/components/JobForm'; import { notFound } from 'next/navigation';
export default async function Edit({params}:{params:Promise<{id:string}>}){await requireAdmin();await connectDb();const {id}=await params;const job=await Job.findById(id).lean();if(!job)notFound();return <main className="mx-auto max-w-4xl p-5 md:p-10"><h1 className="mb-2 text-3xl font-extrabold">Edit job</h1><p className="mb-7 text-sm text-slate-600">Changes are reflected on the public website after saving.</p><JobForm job={JSON.parse(JSON.stringify(job))}/></main>}

