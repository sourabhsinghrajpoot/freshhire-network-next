import { requireAdmin } from '@/lib/auth'; import { connectDb } from '@/lib/db'; import { Job } from '@/models/Job'; import Link from 'next/link';
export const dynamic='force-dynamic';
export default async function Jobs(){await requireAdmin();await connectDb();const jobs=await Job.find().sort({updatedAt:-1}).lean();return <main className="mx-auto max-w-6xl p-5 md:p-10"><header className="mb-8 flex items-start justify-between"><div><p className="font-extrabold text-xl text-blue-700">FreshHire <span className="text-green-600">Network</span></p><h1 className="mt-3 text-3xl font-extrabold">Jobs dashboard</h1><p className="mt-1 text-sm text-slate-600">Update listings whenever you need—published jobs show on the website immediately.</p></div><Link className="btn btn-primary" href="/admin/jobs/new">+ Add job</Link></header><section className="card overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Job</th><th className="p-4">Location</th><th className="p-4">Status</th><th className="p-4">Verified</th><th className="p-4">Action</th></tr></thead><tbody>{jobs.map((j:any)=><tr className="border-b" key={String(j._id)}><td className="p-4"><b>{j.title}</b><br/><span className="text-xs text-slate-500">{j.company}</span></td><td className="p-4">{j.location}</td><td className="p-4"><span className="badge">{j.status}</span></td><td className="p-4">{j.verified?'Yes':'No'}</td><td className="p-4"><Link className="font-bold text-blue-700" href={`/admin/jobs/${j._id}/edit`}>Edit</Link></td></tr>)}</tbody></table>{!jobs.length&&<p className="p-8 text-center text-slate-500">No jobs yet. Add the first listing.</p>}</section></main>}
