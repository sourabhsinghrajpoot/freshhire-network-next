'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function Login(){const router=useRouter();const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(!r.ok){setError('Invalid email or password');return}router.push('/admin/jobs');router.refresh()}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="card w-full max-w-md p-8"><p className="font-extrabold text-xl text-blue-700">FreshHire <span className="text-green-600">Network</span></p><h1 className="mt-5 text-2xl font-extrabold">Admin login</h1><p className="mt-2 text-sm text-slate-600">Manage jobs and official application links.</p><div className="mt-6"><label>Email</label><input type="email" name="email" required/></div><div className="mt-4"><label>Password</label><input type="password" name="password" required/></div>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="btn btn-primary mt-6 w-full">Sign in</button></form></main>}
