import { NextResponse } from 'next/server'; import { isAdmin } from '@/lib/auth'; import { connectDb } from '@/lib/db'; import { Job } from '@/models/Job'; import { jobSchema } from '@/lib/validation';
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const parsed=jobSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0].message},{status:400});await connectDb();const {id}=await params;const job=await Job.findByIdAndUpdate(id,{...parsed.data,deadline:parsed.data.deadline?new Date(parsed.data.deadline):undefined},{new:true,runValidators:true});return job?NextResponse.json(job):NextResponse.json({error:'Not found'},{status:404})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});await connectDb();const {id}=await params;await Job.findByIdAndDelete(id);return new NextResponse(null,{status:204})}
