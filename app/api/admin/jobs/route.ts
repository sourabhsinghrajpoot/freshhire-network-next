import { NextResponse } from 'next/server'; import { isAdmin } from '@/lib/auth'; import { connectDb } from '@/lib/db'; import { Job } from '@/models/Job'; import { jobSchema, makeSlug } from '@/lib/validation';
export async function GET(){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});await connectDb();return NextResponse.json(await Job.find().sort({updatedAt:-1}).lean())}
export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'Unauthorized'},{status:401});const parsed=jobSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0].message},{status:400});await connectDb();const j=await Job.create({...parsed.data,slug:makeSlug(`${parsed.data.title}-${parsed.data.company}`),deadline:parsed.data.deadline?new Date(parsed.data.deadline):undefined});return NextResponse.json(j,{status:201})}

